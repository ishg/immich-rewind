services:
  rewind:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      IMMICH_SERVER_URL: ${IMMICH_SERVER_URL}
      IMMICH_API_KEY: ${IMMICH_API_KEY}
    restart: unless-stopped
